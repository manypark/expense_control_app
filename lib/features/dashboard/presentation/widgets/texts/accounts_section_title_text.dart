import 'package:flutter/material.dart';

class AccountsSectionTitleText extends StatelessWidget {
  const AccountsSectionTitleText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Bancos y fintech',
      style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600),
    );
  }
}
