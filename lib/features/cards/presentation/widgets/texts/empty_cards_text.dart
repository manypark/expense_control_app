import 'package:flutter/material.dart';

class EmptyCardsText extends StatelessWidget {
  const EmptyCardsText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('No hay tarjetas registradas');
  }
}
