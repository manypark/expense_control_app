import 'package:flutter/material.dart';

class EmptyExpensesText extends StatelessWidget {
  const EmptyExpensesText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Aun no hay gastos registrados');
  }
}
